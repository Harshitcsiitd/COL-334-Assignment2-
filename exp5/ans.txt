**Data Analysis: Experiment 5 Run**

This is excellent data. You have successfully captured the multiplexing behavior required to demonstrate readiness-based socket handling. Let's break down the evidence for your report.

**Key Evidence Extracted**

* **The Connected Descriptors (`procstat -f` & `sockstat -4 -c`):**
Your `procstat` and `sockstat` outputs confirm the server successfully held all five TCP connections simultaneously using a single process (`exchange_server`, PID `48312`).


* FD 5 ↔ Port 53560 (Client 1)
* FD 6 ↔ Port 35050 (Client 2)
* FD 7 ↔ Port 37828 (Client 3)
* FD 8 ↔ Port 35120 (Client 4)
* FD 9 ↔ Port 59662 (Client 5)


* **The Multiplexing Proof (`kdump uniq -c`):**
This is the core proof. The `uniq -c` command counted the total number of `recvfrom` calls made on each file descriptor throughout the entire experiment. The results perfectly align with the harness's behavior:


* FD 5 (Client 1) had 8 `recvfrom` calls.
* FD 7 (Client 3) had 6 `recvfrom` calls.
* FD 9 (Client 5) had 6 `recvfrom` calls.
* FD 6 (Client 2) and FD 8 (Client 4) had only **2** `recvfrom` calls.



**Why did FDs 6 and 8 have 2 calls instead of 0?**
If you look closely at the end of the `kdump` trace (`1788921761.296...`), you can see those final `recvfrom` calls returning `0`. These are not data reads; they represent the server processing the `EOF` (FIN) signal when the harness closed the connections at the end of the 15-second observation window. Before that final close event, FDs 6 and 8 were completely ignored by `kevent` while data was actively being read from 5, 7, and 9.

**Answering the Assignment Question**

For your report, use the data above to state that the active clients were Clients 1, 3, and 5. The system evidence for this is twofold:

1. **Syscall Trace:** `kevent` only returned readiness events for FDs 5, 7, and 9 during the active phase, causing the server to issue `recvfrom` calls exclusively on those descriptors to process the `LOGIN` data, while ignoring FDs 6 and 8.


2. **Thread Concurrency:** `procstat` confirms a single thread managed all 5 descriptors, proving that idle connections merely occupy kernel state and do not consume application thread time or polling cycles when using `kqueue`.



Client Connection MappingHarness Client #Ephemeral PortServer FDAppeared in recvfrom trace (kdump.txt)?Client 1535605Yes (8 calls)  Client 2350506Yes (2 calls - teardown only)  Client 3378287Yes (6 calls)  Client 4351208Yes (2 calls - teardown only)  Client 5596629Yes (6 calls)  The active clients (1, 3, and 5) actively consumed CPU cycles by triggering multiple recvfrom system calls to process application messages and handle EAGAIN (Resource temporarily unavailable) states. The idle clients (2 and 4) did not trigger any read events during the observation phase; their only recvfrom calls occurred during the final script termination sequence to read the EOF (returning 0).  